# DevFlow Backend

Backend runtime for DevFlow.

Phase 1 / Step 1.1 contains only the Python package skeleton, settings management, tests, lint configuration, and CI quality baseline. Agent/model integration starts in later steps according to `../docs/DEVELOPMENT_PLAN.md`.
